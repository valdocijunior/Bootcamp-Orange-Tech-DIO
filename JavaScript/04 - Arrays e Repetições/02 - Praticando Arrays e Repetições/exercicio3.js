const nomes = ['Vinicius', 'Jose', 'Renan', 'Jhonny', 'Roberto', 'Valdoci', 'Carlos', 'Lula'];
    for (let i = 0; i < nomes.length; i++) {
        if (nomes[i].charAt(0) === 'V') {
            console.log(nomes[i]);
        }
    }